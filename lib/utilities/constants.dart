import 'package:flutter/material.dart';

class Constants {
  static const borderRadius = Radius.circular(16);
  static const textFieldBorderRadius = BorderRadius.all(Radius.circular(16));

  static const sidePadding = EdgeInsets.only(right: 8.0, left: 8.0);
  static const allPadding = EdgeInsets.all(8.0);
  static const sideTopPadding = EdgeInsets.only(right: 8.0, left: 8, top: 8);
  static const verticalleftPadding =
      EdgeInsets.only(left: 8.0, top: 8, bottom: 8);
}
